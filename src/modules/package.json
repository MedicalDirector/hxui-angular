{
  "name": "@hxui/angular",
  "version": "1.0.0",
  "description": "Native Angular HxUi Components",
  "scripts": {
    "transpile": "ngc",
    "package": "rollup -c",
    "minify": "uglifyjs dist/bundles/hxui.umd.js --screw-ie8 --compress --mangle --comments --output dist/bundles/hxui.umd.min.js",
    "build": "npm run transpile && npm run package && npm run minify"
  },
  "devDependencies": {
    "@angular/compiler": "^5.1.0",
    "@angular/compiler-cli": "^5.1.0",
    "rollup": "^0.52.1",
    "typescript": "^2.6.2",
    "uglify-js": "^3.2.2"
  }
}
